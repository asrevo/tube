package org.revo.Service;

/*
import org.revo.Domain.Media;
import org.revo.Domain.MediaGroup;

import java.util.List;
import java.util.Optional;

public interface MediaGroupService {
    Optional<MediaGroup> findOne(String id);

    void delete(String id);

    MediaGroup save(MediaGroup mediaGroup);

    void addMediaToMediaGroup(MediaGroup mediaGroup, String media);

    List<Media> GetAllMedia(MediaGroup mediaGroup);

    Media store(Media media);
}
*/
